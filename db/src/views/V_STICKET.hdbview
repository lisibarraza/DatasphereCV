VIEW V_STICKET
("CARRID", "CONNID", "FLDATE", "BOOKID", "CUSTOMID", "TICKET" ,"PLACE", "ARCHIVE_")
AS
SELECT "CARRID", "CONNID", "FLDATE", "BOOKID", "CUSTOMID", "TICKET" ,"PLACE", "ARCHIVE_"
FROM "SYN_STICKET"
WHERE "MANDT" = 300