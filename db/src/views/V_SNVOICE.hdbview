VIEW V_SNVOICE
("CARRID", "CONNID", "FLDATE", "BOOKID", "CUSTOMID", "INSTNO", "PAYMETH", "AMOUNT", "CURRENCY", "ARCHIVE_")
AS
SELECT "CARRID", "CONNID", "FLDATE", "BOOKID", "CUSTOMID", "INSTNO", "PAYMETH", "AMOUNT", "CURRENCY", "ARCHIVE_"
FROM "SYN_SNVOICE"
WHERE "MANDT" = 300